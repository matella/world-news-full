[workspace]
members = [
    "backend/api-gateway",
    "backend/scraper",
    "backend/ai-service",
]

# Use the edition 2024 resolver which is required for workspace members on
# edition = "2024". This prevents the warning about resolver defaulting to 1.
resolver = "3"
